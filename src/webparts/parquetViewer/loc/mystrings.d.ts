declare interface IParquetViewerWebPartStrings {
  PropertyPaneDescription: string;
  BasicGroupName: string;
  FilePathFieldLabel: string;
}

declare module 'ParquetViewerWebPartStrings' {
  const strings: IParquetViewerWebPartStrings;
  export = strings;
}
